10: long score;

struct Character {
  100: int xp;

  200: int hp;
  201: int max_hp;

  205: int mp;
  206: int max_mp;
}

1000: Character[] party;

// effects a .story may invoke on-enter; each maps to a C
// void effect_<name>(spine_t* doc, int param) at codegen.
effect give_crown;
effect mark_greedy;
effect kill_player;
effect resolve_vault;

effect give_xp;